package org.ryan.chapter2;

public class ArrayDataStruct {
    /**
     * 数组数据结构
     * 优点：支持随机访问，查询速度快
     * 缺点：插入和删除操作比较慢，因为插入和删除操作都要移动其他数据（因为内存在给数组分配内存时要分配一块连续的内存区域）
     * 时间复杂度：
     * 查询：O(1)
     * 插入、删除：O(n)
     */
}
